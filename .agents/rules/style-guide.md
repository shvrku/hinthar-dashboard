---
trigger: always_on
---

Follow the shadcn styling for all components being made and follow the colors used in the project. Always use shadcn components. Project install is **base-vega / zinc** (`components.json`). You have access to MCP — use it if it is not accessible, state it without continuing further. 